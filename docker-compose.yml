services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: crypto_quant
      POSTGRES_USER: crypto_quant
      POSTGRES_PASSWORD: crypto_quant
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
